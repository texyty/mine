/*
 * Local and Docker Compose defaults. Vercel replaces this file during `npm run build`
 * using the public LAUNCHER_API environment variable.
 */
window.RUNTIME_CONFIG = Object.freeze({ apiBaseUrl: "" });
